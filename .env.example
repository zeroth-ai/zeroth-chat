# DATABASE CONNECTION (Supabase)
# Connect to your Supabase PostgreSQL instance.
# If deploying to Vercel, use the "Transaction Pooler" port (6543) and append "?pgbouncer=true"
DATABASE_URL="postgresql://postgres:[PASSWORD]@db.supabase.co:6543/postgres?pgbouncer=true"

# AI MODEL PROVIDER (Hugging Face)
# Your access token from huggingface.co/settings/tokens
# Required to access the Llama-3.2-11B-Vision-Instruct model.
HF_TOKEN="hf_your_token_here"

# APP SECURITY (Optional For Deployment)
# These credentials protect your deployed app with a password popup.
BASIC_AUTH_USER="admin"
BASIC_AUTH_PASS="change_this_password"